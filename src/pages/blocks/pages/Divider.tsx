/**
 * /blocks/divider — Divider 块专页（blocks.md 固定模板，实现见 ../BlockDocPage）。
 */
import BlockDocPage from "../BlockDocPage";

export default function DividerPage() {
  return <BlockDocPage slug="divider" />;
}
