/**
 * /blocks/heading — Heading 块专页（blocks.md 固定模板，实现见 ../BlockDocPage）。
 */
import BlockDocPage from "../BlockDocPage";

export default function HeadingPage() {
  return <BlockDocPage slug="heading" />;
}
